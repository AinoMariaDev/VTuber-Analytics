# Web化ステップ1 保存先切り替え基盤

何も設定しなければ従来の保存先を使用します。
サーバー移行時は `VTA_DB_PATH` などの環境変数で永続ディスクを指定できます。

主な変数: `VTA_DATA_DIR`, `VTA_DB_PATH`, `VTA_CONFIG_PATH`, `VTA_CHAT_DIR`, `VTA_BACKUP_DIR`, `VTA_WEEKLY_SCHEDULE_DIR`
